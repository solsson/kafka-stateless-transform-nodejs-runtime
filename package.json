{
  "name": "a-stateless-transform-nodejs",
  "version": "1.0.0",
  "description": "Example function that adheres to the spec for our message transform example",
  "main": "index.js",
  "scripts": {
    "test": "node -e '(async function() { process.stdout.write(JSON.stringify(await require(\"./\")({test:true}))); })()' | grep }}"
  },
  "author": "",
  "license": "ISC"
}
